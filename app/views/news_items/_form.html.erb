<%= form_with(model: news_item, local: true) do |form| %>
  <% if news_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(news_item.errors.count, "error") %> prohibited this news_item from being saved:</h2>

      <ul>
      <% news_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :players %>
    <%= form.select :player_id, @players.collect { |player| [player.name, player.id] }, prompt: 'Select Player', :selected => params['player_id']  %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :news_item_body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
